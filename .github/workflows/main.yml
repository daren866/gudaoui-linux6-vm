name: Build Linux Kernel 6.18 with BusyBox

on:
  push:
    branches: [ "main", "master" ]
  workflow_dispatch:

# 添加必要的权限
permissions:
  contents: write
  actions: read

jobs:
  build:
    runs-on: ubuntu-latest
    
    outputs:
      release_title: ${{ steps.set-release-info.outputs.release_title }}
      release_tag: ${{ steps.set-release-info.outputs.release_tag }}
      iso_path: ${{ steps.set-release-info.outputs.iso_path }}
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    # ===== 设置Release信息 =====
    - name: Set release info
      id: set-release-info
      run: |
        # 获取Unix时间戳
        UNIX_TIMESTAMP=$(date +%s)
        echo "Unix时间戳: $UNIX_TIMESTAMP"
        
        # 计算乘以9178666后的数字
        RELEASE_NUMBER=$((UNIX_TIMESTAMP * 9178666))
        echo "Release数字: $RELEASE_NUMBER"
        
        # 生成Release标题
        RELEASE_TITLE="Linux6 虚拟机版-${RELEASE_NUMBER}"
        echo "Release标题: $RELEASE_TITLE"
        
        # 生成ISO文件名
        ISO_NAME="linux6-vm-${UNIX_TIMESTAMP}.iso"
        
        # 设置输出变量
        echo "release_title=$RELEASE_TITLE" >> $GITHUB_OUTPUT
        echo "release_tag=linux6-vm-${UNIX_TIMESTAMP}" >> $GITHUB_OUTPUT
        echo "iso_name=$ISO_NAME" >> $GITHUB_OUTPUT
        echo "release_number=$RELEASE_NUMBER" >> $GITHUB_OUTPUT
        
        # 保存到环境变量
        echo "RELEASE_TITLE=$RELEASE_TITLE" >> $GITHUB_ENV
        echo "ISO_NAME=$ISO_NAME" >> $GITHUB_ENV

    # ===== 🚀 内核缓存 =====
    - name: Cache kernel build
      uses: actions/cache@v4
      id: kernel-cache
      with:
        path: linux-6.18
        key: kernel-6.18-${{ hashFiles('6.18.x-x86_64-build.conf') }}
        restore-keys: |
          kernel-6.18-

    # ===== BusyBox缓存 =====
    - name: Cache busybox build
      uses: actions/cache@v4
      id: busybox-cache
      with:
        path: busybox-1.36.1
        key: busybox-1.36.1-${{ hashFiles('busybox-config.conf') }}
        restore-keys: |
          busybox-1.36.1-

    - name: Check cache status
      run: |
        echo "## 🔍 缓存状态" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        
        echo "### 内核缓存" >> $GITHUB_STEP_SUMMARY
        if [[ "${{ steps.kernel-cache.outputs.cache-hit }}" == "true" ]]; then
          echo "✅ **内核缓存命中！**" >> $GITHUB_STEP_SUMMARY
          echo "缓存Key: \`${{ steps.kernel-cache.outputs.primary-key }}\`" >> $GITHUB_STEP_SUMMARY
        else
          echo "❌ 内核缓存未命中" >> $GITHUB_STEP_SUMMARY
        fi
        
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "### BusyBox缓存" >> $GITHUB_STEP_SUMMARY
        if [[ "${{ steps.busybox-cache.outputs.cache-hit }}" == "true" ]]; then
          echo "✅ **BusyBox缓存命中！**" >> $GITHUB_STEP_SUMMARY
          echo "缓存Key: \`${{ steps.busybox-cache.outputs.primary-key }}\`" >> $GITHUB_STEP_SUMMARY
        else
          echo "❌ BusyBox缓存未命中" >> $GITHUB_STEP_SUMMARY
        fi

    - name: Install dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y \
          build-essential \
          libncurses-dev \
          bison \
          flex \
          libssl-dev \
          libelf-dev \
          bc \
          wget \
          cpio \
          xorriso \
          grub-pc-bin \
          grub-common \
          mtools \
          dosfstools \
          upx \
          python3-minimal \
          python3-venv

    # ===== 内核部分 =====
    - name: Download kernel source
      if: steps.kernel-cache.outputs.cache-hit != 'true'
      run: |
        echo "📥 下载内核源码 linux-6.18..."
        wget -q https://cdn.kernel.org/pub/linux/kernel/v6.x/linux-6.18.tar.xz
        tar -xf linux-6.18.tar.xz
        echo "✅ 内核源码下载完成"

    - name: Configure kernel
      run: |
        cd linux-6.18
        
        # 下载配置文件
        echo "📝 配置内核..."
        wget -q -O .config.original https://raw.githubusercontent.com/puppylinux-woof-CE/woof-CE/testing/kernel-kit/configs_x86_64/DOTconfig-6.18.1-x86_64 || true
        
        if [ -f .config.original ]; then
          cp .config.original .config
        else
          make defconfig
        fi
        
        # 应用 VM 优化
        echo "🔧 应用虚拟机优化..."
        scripts/config --enable VIRTIO
        scripts/config --enable VIRTIO_PCI
        scripts/config --enable VIRTIO_BLK
        scripts/config --enable VIRTIO_NET
        scripts/config --enable VMXNET3
        scripts/config --enable E1000
        scripts/config --enable DRM_VMWGFX
        scripts/config --disable SOUND
        scripts/config --disable USB_SUPPORT
        scripts/config --disable WLAN
        
        # 启用电源管理相关选项
        scripts/config --enable POWER_SUPPLY
        scripts/config --enable SUSPEND
        scripts/config --enable HIBERNATION
        scripts/config --enable ACPI
        scripts/config --enable ACPI_BUTTON
        scripts/config --enable ACPI_POWER_METER
        scripts/config --enable ACPI_PROC_EVENT
        scripts/config --enable ACPI_SYSTEM_POWER_STATES_SUPPORT
        
        make olddefconfig

    - name: Build kernel
      run: |
        cd linux-6.18
        
        if [ -f "arch/x86/boot/bzImage" ] && [ "${{ steps.kernel-cache.outputs.cache-hit }}" == "true" ]; then
          echo "✅ 使用缓存的内核，跳过编译"
        else
          echo "🔨 编译内核中（这需要约30分钟）..."
          start_time=$(date +%s)
          
          make -j$(nproc) bzImage
          make -j$(nproc) modules
          
          end_time=$(date +%s)
          duration=$((end_time - start_time))
          echo "✅ 内核编译完成，耗时: $((duration / 60)) 分钟"
        fi
        
        echo "内核位置: $(pwd)/arch/x86/boot/bzImage"
        ls -lh arch/x86/boot/bzImage

    # ===== BusyBox 部分 =====
    - name: Download BusyBox
      if: steps.busybox-cache.outputs.cache-hit != 'true'
      run: |
        echo "📥 下载 BusyBox 1.36.1..."
        wget -q https://busybox.net/downloads/busybox-1.36.1.tar.bz2
        tar -xf busybox-1.36.1.tar.bz2
        echo "✅ BusyBox 下载完成"

    - name: Configure BusyBox
      run: |
        cd busybox-1.36.1
        
        echo "🔧 配置 BusyBox..."
        make defconfig
        
        # 静态编译
        sed -i 's/# CONFIG_STATIC is not set/CONFIG_STATIC=y/' .config
        
        # 启用自定义脚本支持
        sed -i 's/# CONFIG_ASH is not set/CONFIG_ASH=y/' .config
        sed -i 's/# CONFIG_SH is not set/CONFIG_SH=y/' .config
        sed -i 's/# CONFIG_ASH_MATH_SUPPORT is not set/CONFIG_ASH_MATH_SUPPORT=y/' .config
        sed -i 's/# CONFIG_ASH_MATH_SUPPORT_64 is not set/CONFIG_ASH_MATH_SUPPORT_64=y/' .config
        
        # 启用电源管理命令
        sed -i 's/# CONFIG_REBOOT is not set/CONFIG_REBOOT=y/' .config
        sed -i 's/# CONFIG_HALT is not set/CONFIG_HALT=y/' .config
        sed -i 's/# CONFIG_POWEROFF is not set/CONFIG_POWEROFF=y/' .config
        
        # 禁用 TC
        sed -i 's/CONFIG_TC=y/# CONFIG_TC is not set/' .config 2>/dev/null || true
        
        # 启用实用功能
        sed -i 's/# CONFIG_FEATURE_PS_LONG is not set/CONFIG_FEATURE_PS_LONG=y/' .config
        sed -i 's/# CONFIG_FEATURE_TOP_CPU_USAGE_PERCENTAGE is not set/CONFIG_FEATURE_TOP_CPU_USAGE_PERCENTAGE=y/' .config
        sed -i 's/# CONFIG_FEATURE_SHUTDOWN_LOGIN is not set/CONFIG_FEATURE_SHUTDOWN_LOGIN=y/' .config
        
        make oldconfig

    - name: Build BusyBox
      run: |
        cd busybox-1.36.1
        
        if [ -f "busybox" ] && [ "${{ steps.busybox-cache.outputs.cache-hit }}" == "true" ]; then
          echo "✅ 使用缓存的 BusyBox，跳过编译"
        else
          echo "🔨 编译 BusyBox 中..."
          make -j$(nproc)
          make install
          echo "✅ BusyBox 编译完成"
        fi
        
        ls -lh busybox

    # ===== 准备最小化 Python =====
    - name: Prepare minimal Python
      run: |
        echo "🐍 准备最小化 Python..."
        
        # 创建 Python 目录
        mkdir -p python-minimal/bin python-minimal/lib
        
        # 从系统复制最小化 Python
        cp /usr/bin/python3 python-minimal/bin/
        cp /usr/bin/pip3 python-minimal/bin/ 2>/dev/null || true
        
        # 复制必要的动态库
        ldd /usr/bin/python3 | grep "=> /" | awk '{print $3}' | xargs -I {} cp {} python-minimal/lib/ 2>/dev/null || true
        
        # 复制 Python 标准库（最小化）
        mkdir -p python-minimal/lib/python3.12
        cp -r /usr/lib/python3.12/*.py python-minimal/lib/python3.12/ 2>/dev/null || true
        cp -r /usr/lib/python3.12/collections python-minimal/lib/python3.12/ 2>/dev/null || true
        cp -r /usr/lib/python3.12/_collections_abc.py python-minimal/lib/python3.12/ 2>/dev/null || true
        cp -r /usr/lib/python3.12/abc.py python-minimal/lib/python3.12/ 2>/dev/null || true
        cp -r /usr/lib/python3.12/sys.py python-minimal/lib/python3.12/ 2>/dev/null || true
        cp -r /usr/lib/python3.12/types.py python-minimal/lib/python3.12/ 2>/dev/null || true
        cp -r /usr/lib/python3.12/io.py python-minimal/lib/python3.12/ 2>/dev/null || true
        cp -r /usr/lib/python3.12/os.py python-minimal/lib/python3.12/ 2>/dev/null || true
        cp -r /usr/lib/python3.12/platform.py python-minimal/lib/python3.12/ 2>/dev/null || true
        
        # 使用 UPX 压缩 Python 二进制文件（如果可用）
        if command -v upx >/dev/null 2>&1; then
            upx --best python-minimal/bin/python3 2>/dev/null || true
        fi
        
        echo "✅ 最小化 Python 准备完成"
        echo "Python 大小: $(du -sh python-minimal | cut -f1)"

    # ===== 创建 rootfs =====
    - name: Create rootfs
      run: |
        echo "📦 创建 rootfs..."
        
        # 创建目录结构
        mkdir -p rootfs/{bin,sbin,usr/bin,usr/sbin,etc,proc,sys,dev,lib,lib64,tmp,var,run,root,usr/local/bin,usr/lib,home}
        
        # 复制 BusyBox
        cp -a busybox-1.36.1/_install/* rootfs/
        
        # 复制最小化 Python
        cp -a python-minimal/* rootfs/usr/
        
        # 创建 Python 符号链接
        cd rootfs/usr/bin
        ln -sf python3 python
        cd ../../..
        
        # 创建设备节点
        sudo mknod -m 666 rootfs/dev/null c 1 3
        sudo mknod -m 644 rootfs/dev/console c 5 1
        sudo mknod -m 660 rootfs/dev/tty1 c 4 1
        sudo mknod -m 660 rootfs/dev/tty2 c 4 2
        sudo mknod -m 660 rootfs/dev/tty3 c 4 3
        sudo mknod -m 660 rootfs/dev/tty4 c 4 4
        
        # 创建 about 命令脚本
        cat > rootfs/usr/local/bin/about << 'EOF'
        #!/bin/sh
        # about command - display system information
        
        echo "GudaoUI linux V1 (Minimal Python)"
        echo "myweb: gudaoweb.pages.dev"
        echo "linux: $(uname -r)"
        if command -v python3 >/dev/null 2>&1; then
            echo "python: $(python3 --version 2>&1)"
        else
            echo "python: not available"
        fi
        EOF
        
        chmod +x rootfs/usr/local/bin/about
        
        # 创建 calc 命令脚本
        cat > rootfs/usr/local/bin/calc << 'EOF'
        #!/bin/sh
        # calc command - simple calculator
        
        if [ $# -eq 0 ]; then
            echo "Usage: calc <expression>"
            echo "Example: calc 1+1"
            echo "Example: calc (2*2)+2"
            exit 1
        fi
        
        expression="$*"
        result=$(awk "BEGIN {print $expression}")
        
        if [ "$result" = "${result%.*}" ]; then
            echo "$result"
        else
            printf "%.2f\n" "$result"
        fi
        EOF
        
        chmod +x rootfs/usr/local/bin/calc
        
        # 创建最小化 Python 测试脚本
        cat > rootfs/home/test.py << 'EOF'
        #!/usr/bin/env python3
        # Minimal Python test script
        
        import sys
        
        print("=" * 40)
        print("Minimal Python Test")
        print("=" * 40)
        print(f"Python version: {sys.version.split()[0]}")
        print(f"Python executable: {sys.executable}")
        print("=" * 40)
        
        # Simple calculations
        print("\nCalculations:")
        print(f"1 + 2 = {1 + 2}")
        print(f"3 * 4 = {3 * 4}")
        print(f"10 / 3 = {10 / 3:.2f}")
        
        print("\nMinimal Python is working!")
        EOF
        
        chmod +x rootfs/home/test.py
        
        # 创建 Python 计算器脚本
        cat > rootfs/usr/local/bin/pycalc << 'EOF'
        #!/usr/bin/env python3
        # Minimal Python calculator
        
        import sys
        
        if len(sys.argv) < 2:
            print("Usage: pycalc <expression>")
            print("Example: pycalc 1+2")
            sys.exit(1)
        
        try:
            expr = ' '.join(sys.argv[1:])
            result = eval(expr)
            print(result)
        except Exception as e:
            print(f"Error: {e}")
        EOF
        
        chmod +x rootfs/usr/local/bin/pycalc
        
        # 创建 reboot/poweroff 脚本
        cat > rootfs/usr/local/bin/reboot << 'EOF'
        #!/bin/sh
        echo "System is rebooting..."
        sync
        /bin/busybox reboot -f
        EOF
        
        chmod +x rootfs/usr/local/bin/reboot
        
        cat > rootfs/usr/local/bin/poweroff << 'EOF'
        #!/bin/sh
        echo "System is powering off..."
        sync
        /bin/busybox poweroff -f
        EOF
        
        chmod +x rootfs/usr/local/bin/poweroff
        
        # 创建符号链接
        cd rootfs/usr/bin
        ln -sf ../local/bin/about about
        ln -sf ../local/bin/calc calc
        ln -sf ../local/bin/reboot reboot
        ln -sf ../local/bin/poweroff poweroff
        ln -sf poweroff halt
        ln -sf ../local/bin/pycalc pycalc
        cd ../../..
        
        # 创建 init 脚本
        cat > rootfs/init << 'EOF'
        #!/bin/busybox sh
        # 初始化脚本 - 最小化版本
        
        # 设置 PATH
        export PATH=/bin:/sbin:/usr/bin:/usr/sbin:/usr/local/bin
        export LD_LIBRARY_PATH=/usr/lib:$LD_LIBRARY_PATH
        
        # 挂载文件系统
        /bin/busybox mount -t proc none /proc
        /bin/busybox mount -t sysfs none /sys
        /bin/busybox mount -t devtmpfs none /dev
        /bin/busybox mount -t tmpfs tmpfs /tmp
        /bin/busybox mount -t tmpfs tmpfs /run
        /bin/busybox mount -t tmpfs tmpfs /dev/shm
        
        # 创建必要的目录
        /bin/busybox mkdir -p /dev/shm /run/lock
        
        # 创建必要的设备节点
        /bin/busybox mknod -m 660 /dev/tty1 c 4 1 2>/dev/null || true
        /bin/busybox mknod -m 660 /dev/tty2 c 4 2 2>/dev/null || true
        
        # 设置主机名
        /bin/busybox echo "gudao-ui" > /proc/sys/kernel/hostname 2>/dev/null || true
        
        # 配置网络
        /bin/busybox ip link set lo up 2>/dev/null || true
        
        # 安装 BusyBox 应用
        /bin/busybox --install -s
        
        # 获取内存大小
        MEM_SIZE=$(grep MemTotal /proc/meminfo 2>/dev/null | awk '{print int($2/1024)}')
        [ -z "$MEM_SIZE" ] && MEM_SIZE="Unknown"
        
        # 清屏
        clear
        
        # 显示启动画面
        echo "====================="
        echo "GudaoUI Linux V1"
        echo "====================="
        echo "Enter 'about' to query system information"
        echo "Enter 'calc' for calculator"
        echo "Enter 'pycalc' for Python calculator"
        echo "Enter 'python' for Python REPL"
        echo "Enter 'reboot' to restart"
        echo "Enter 'poweroff' to shut down"
        echo "Running memory size: ${MEM_SIZE}MB"
        echo ""
        
        # 显示 Python 信息
        if command -v python3 >/dev/null 2>&1; then
            echo "Python: $(python3 --version 2>&1) (minimal)"
        fi
        echo ""
        
        echo "Welcome to GudaoUI Linux! Type 'exit' to logout."
        echo ""
        
        # 启动 shell
        exec /bin/sh
        EOF
        
        chmod +x rootfs/init
        
        # 创建基本配置文件
        echo "root:x:0:0:root:/root:/bin/sh" > rootfs/etc/passwd
        echo "root:x:0:" > rootfs/etc/group
        echo "nameserver 8.8.8.8" > rootfs/etc/resolv.conf
        echo "127.0.0.1 localhost gudao-ui" > rootfs/etc/hosts
        
        # 创建 fstab
        cat > rootfs/etc/fstab << 'EOF'
        proc    /proc    proc    defaults    0 0
        sysfs   /sys     sysfs   defaults    0 0
        devtmpfs /dev    devtmpfs defaults  0 0
        tmpfs   /tmp     tmpfs   defaults    0 0
        tmpfs   /run     tmpfs   defaults    0 0
        EOF
        
        # 创建 profile
        cat > rootfs/etc/profile << 'EOF'
        export PATH=/bin:/sbin:/usr/bin:/usr/sbin:/usr/local/bin
        export LD_LIBRARY_PATH=/usr/lib:$LD_LIBRARY_PATH
        export PS1='\u@\h:\w\$ '
        alias ll='ls -la'
        alias python='python3'
        EOF
        
        # 打包 rootfs
        cd rootfs
        find . | cpio -o -H newc > ../rootfs.cpio
        cd ..
        
        echo "rootfs.cpio 大小: $(du -h rootfs.cpio | cut -f1)"
        echo "Python 大小: $(du -sh rootfs/usr/bin/python3 | cut -f1)"

    # ===== 创建 ISO =====
    - name: Create bootable ISO
      run: |
        echo "💿 创建启动 ISO..."
        
        mkdir -p iso/boot/grub
        
        cp linux-6.18/arch/x86/boot/bzImage iso/boot/vmlinuz
        cp rootfs.cpio iso/boot/initramfs.cpio
        
        cat > iso/boot/grub/grub.cfg << EOF
        set default=0
        set timeout=3
        
        menuentry "GudaoUI Linux V1 (Minimal Python)" {
          linux /boot/vmlinuz quiet console=tty0
          initrd /boot/initramfs.cpio
        }
        EOF
        
        grub-mkrescue -o "$ISO_NAME" iso/
        
        echo "✅ ISO 创建完成: $ISO_NAME"
        echo "ISO 大小: $(du -h $ISO_NAME | cut -f1)"

    # ===== 上传 ISO =====
    - name: Upload ISO to Artifacts
      uses: actions/upload-artifact@v4
      with:
        name: ${{ steps.set-release-info.outputs.iso_name }}
        path: ${{ steps.set-release-info.outputs.iso_name }}
        retention-days: 30

    # ===== 创建 Release =====
    - name: Create Release
      uses: softprops/action-gh-release@v1
      with:
        name: ${{ steps.set-release-info.outputs.release_title }}
        tag_name: ${{ steps.set-release-info.outputs.release_tag }}
        body: |
          ## 🎉 GudaoUI Linux V1 (Minimal Python)
          
          ### 📦 包含组件
          - Linux 内核 6.18
          - BusyBox 1.36.1
          - **最小化 Python 3.12**
          - about/calc/pycalc 命令
          
          ### 💻 使用说明
          - `about` - 系统信息
          - `calc 1+1` - 计算器
          - `pycalc 1+2` - Python计算器
          - `python` - Python REPL
          - `python /home/test.py` - 测试脚本
        files: |
          ${{ steps.set-release-info.outputs.iso_name }}
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
